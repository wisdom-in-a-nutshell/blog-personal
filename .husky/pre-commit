#!/bin/sh
# Format staged files with Ultracite (recommended minimal hook)
set -e

# If nothing staged, exit fast
if [ -z "$(git diff --cached --name-only)" ]; then
  exit 0
fi

# Format and fix the codebase using Ultracite
pnpm dlx ultracite fix
